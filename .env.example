# Inspect MCP Server Configuration

# Chrome display mode
# Set to any value (e.g., 'true') to run Chrome in headless mode (no visible window)
# Leave unset or comment out to run Chrome with visible window (default)
# HEADLESS=true

# Example usage:
# For visible Chrome window (default):
#   npm start
# 
# For headless Chrome:
#   HEADLESS=true npm start